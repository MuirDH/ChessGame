
// simple class to represent a specific slot on the board
public class Position {
    int row;
    int column;

    // Constructor using row and column values
    Position (int row, int column) {
        this.row = row;
        this.column = column;
    }
}
